import { PageShell } from '@/components/PageShell';

export default function SearchPage() {
  return <PageShell title="Search holidays" text="The full search experience will be added after the homepage visual foundation is approved." />;
}
